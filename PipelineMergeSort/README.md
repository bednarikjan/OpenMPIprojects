# Pipeline Merge Sort

The implementaion of the parallel algorithm Pipeline Merge Sort using OpenMPI library.

## Dependencies
* OpenMPI (mpic++)

## Compile and Run
```
$ cd src
$ chmod +x test.sh
$ ./test.sh
```
