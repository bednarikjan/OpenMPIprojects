# Pipeline Merge Sort

The implementaion of the parallel algorithm Pipeline Merge Sort using OpenMPI ibrary.

## Dependencies
* OpenMPI (mpic++)

## Run
```
$ cd src
$ chmod +x test.sh
$ ./test.sh
```
